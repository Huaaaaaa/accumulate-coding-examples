简单工厂模式，又叫静态方法模式
1、简单工厂的场景：
现在有一个云厂商，提供主机、硬盘、ip、域名等云产品，客户从云厂商手中购买云产品

2、实现方式：
提供一个云工厂，客户每次从这个工厂中获取自己想要的产品，只要提供要购买的产品标识即可

3、实现并测试
见code

4、分析
优点：当在多个地方需要创建不同的云服务，即调用其service()方法时，通过提供这个简单工厂，避免了产生过多重复创建对象的过程
缺点：
①当新增一种云产品时，需要增加一个新产品的同时要修改工厂中的代码
②如果新增一个产品条件，比如不同地区的云产品（如华北、华南这种地域），则改动较大
以上两点违背了OCP（开闭）原则

5、适用场景
创建不同对象比较频繁，且条件不易改变时，可以选择这种模式，如开发过程中很多地方都会用到以下代码
List list = new ArrayList();
Map map = new HashMap();
Set set = new HashSet();
这种完全可以通过一个简单工厂模式来实现，工厂提供不同集合的创建